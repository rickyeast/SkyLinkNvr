# Docker Environment Configuration for Skylink NVR

# Database Configuration (for host network mode)
DATABASE_URL=postgresql://skylink:your_secure_password@127.0.0.1:5432/skylink_nvr

# PostgreSQL Configuration
POSTGRES_PASSWORD=your_secure_password

# Host System Monitoring (Docker host network mode)
HOST_PROC=/host/proc
HOST_SYS=/host/sys

# Recording Storage Path
RECORDINGS_PATH=/app/recordings

# Optional: External Database URL (if not using bundled PostgreSQL)
# DATABASE_URL=postgresql://user:password@external-db-host:5432/skylink_nvr

# Optional: Redis for Caching (if using external Redis)
# REDIS_URL=redis://localhost:6379

# Environment
NODE_ENV=production